pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               // echo 'Hello World'
               git branch: 'main', url: 'https://github.com/nanbod/ncpl_project_1.git'
            }
        }
        stage('Deploy') {
            steps {
                    sshagent(['ID']) {
                                sh "cp -f * azureuser@51.8.114.49:/var/www/html"
                    }
            }
        }
    }
}
